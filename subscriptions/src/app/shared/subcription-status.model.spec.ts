import { SubcriptionStatus } from './subcription-status.model';

describe('SubcriptionStatus', () => {
  it('should create an instance', () => {
    expect(new SubcriptionStatus()).toBeTruthy();
  });
});
